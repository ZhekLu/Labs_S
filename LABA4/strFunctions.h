#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#define TRUE 1
#define FALSE 0 

char* GetWords(/*@null@*/char* str, char determine);
int ToDecimal(char* number);
int is_eight_digit(char* number);
int Length(char* str);
